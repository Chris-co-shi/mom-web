# Web prototypes

Prototype assets are mandatory design inputs.

Directory convention:

```text
docs/prototypes/web/<vertical-slice>/
├── flow.md
├── desktop-overview.png
├── desktop-detail.png
├── dialogs.png
└── states.png
```

Every prototype must show loading, empty, forbidden, failed, duplicate-submit, and recovery states where applicable.
