# User flows

Each vertical slice stores its role-specific flow here before page implementation.

Required notation:

```text
actor → entry → decision → command → result → recovery
```

VS-01 starts with supplier delivery, incoming inspection, receiving, production execution, finished-goods release, automated putaway, shipment, and traceability.
