# ADR-003: Prototype-first delivery

## Status

Accepted.

## Decision

A page cannot enter implementation until its user flow, desktop prototype, state matrix, component mapping, API mapping, permission requirement, and error states are documented.

Prototype images are versioned under `docs/prototypes/web/<slice>/`.
